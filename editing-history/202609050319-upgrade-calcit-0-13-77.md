# Upgrade corokia to Calcit 0.13.77

Agent: `codex-01a06d75-fd53-7c21-a61b-6966447aee4d-upgrades-6f2b`

- Recovered the runnable legacy Snapshot from `compact.cirru`, migrated its
  macro contracts with the supported 0.13.54 → 0.13.51 → 0.13.77 formatter
  chain, and retired the duplicate legacy Snapshot after validation.
- Added `deps.cirru` for Calcit 0.13.77. `calcit-paint` is pinned to the exact
  merged upgrade commit until a compatible release tag is published; `lilac`
  and `memof` retain their latest declared releases.
- Migrated removed `turn-keyword` and frame-oriented memof APIs to `turn-tag`
  and the current `memof.once` API.
- Replaced legacy required-field Map shorthand with the documented Option API.
  The component/event model is intentionally open Map data, so `open-field`
  forms one reviewed `Dynamic → Dynamic` compatibility boundary and preserves
  the former missing-field-as-nil behavior. Six attached tests cover that
  boundary and the pure pair/length operations.
- Made list `first`/`last` Option handling explicit, migrated paint path command
  rows, completed the event `cond`, and added a concrete Number assertion for
  `.format` dispatch.
- Added a per-definition quality baseline for the pre-existing untyped API and
  CI that checks canonical formatting, preprocessing, dynamic dispatch,
  analysis budgets, and the pure test suite without launching a GUI.

The canonical Snapshot SHA-256 at this checkpoint is
`51bd793824c954ad421c7e85140e1592131737b64f51e6f211726c1ce4ad3cda`.

Local exact-0.13.77 validation passed for normal and `--warn-dyn-method`
check-only modes, all six attached tests, and the quality baseline. A clean Caps
native realization is deferred to Linux CI because the upstream Skia 0.97.2
macOS arm64 binary URL returns 404 and this host does not have Ninja for its
fallback source build.

中文：本次迁移保留开放 Map 组件 API 与字段缺失返回 nil 的旧语义，将 Snapshot、
宏、memof、Option 和 path command 更新到 0.13.77，并以逐 definition baseline、
六个纯函数测试和不启动 GUI 的 CI 固化现状。
